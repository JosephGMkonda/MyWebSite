{
    "builds": [{
        "src": "MyWeb/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.2" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "MyWeb/wsgi.py"
        }
    ]
}